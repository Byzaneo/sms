language: java
# use Java 10
jdk:
- oraclejdk10

install: true

before_install: "git clone https://sources.byzaneo.com/scm/one/maven.git target"
script: "mvn compile --settings target/travis/settings.xml"
